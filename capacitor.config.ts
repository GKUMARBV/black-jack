import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'bj.gj',
  appName: 'Black Jack Track',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
